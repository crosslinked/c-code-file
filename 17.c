#include <stdio.h>
#include<limits.h>
#include<float.h>

int main()
{
    printf("rishabh jain");
    printf("\n char store value from%d to %d",CHAR_MIN,CHAR_MAX);
    printf("\n Integers store the value from %d to %d",INT_MIN,INT_MAX);
    printf("\n The long store the value from %d to %d",LONG_MIN,LONG_MAX);

    return 0;
}