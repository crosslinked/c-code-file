#include <stdio.h>
#include<string.h>

int main()
{ int x;
char y;
float z;
    printf("rishabh jain");
printf("\n The size of integer is %d",sizeof(z));    
printf("\n The size of character is %d",sizeof(y));    
printf("\n The size of float is %d ",sizeof(x));    
    return 0;
}